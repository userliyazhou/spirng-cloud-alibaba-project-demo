spring:
  application:
    name: nacos-config
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        # 如果是 .properties文件 不需要这个配置
        file-extension: yaml

server:
  port: 8082